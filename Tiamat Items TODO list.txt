* GUI

Fix issue where item layers don't appear if GUI is opened while maximized (but are fine if GUI is opened while windowed)

Re-add layer editing

Use arraylist for texture selection, with existing white-layer textures in alphabetical:index order

Add caching options to GUI
Add weapon/armor slot tags to GUI (from tiamatrpg; see TiamatItem class)
Add attributes to GUI
Add affixes to GUI



* RENDER

Improve render via back-to-front voxel ordering?  Not sure how to determine which direction to move in for in-world renders...
